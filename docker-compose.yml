services:
  postgres:
    image: postgres:15-alpine
    env_file:
      - ./.env
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    env_file:
      - ./management/.env
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio-data:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s

  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.15.3
    env_file:
      - ./elasticsearch/.env
    ports:
      - "9200:9200"
    volumes:
      - esdata:/usr/share/elasticsearch/data
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "curl -fs http://localhost:9200/_cluster/health || exit 1"
        ]
      interval: 20s
      timeout: 10s
      retries: 10
      start_period: 30s

  management:
    build:
      context: ./management
      dockerfile: Dockerfile
    command: python manage.py runserver 0.0.0.0:8000
    env_file:
      - ./management/.env
    volumes:
      - ./management:/app
    ports:
      - "8002:8000"
    depends_on:
      postgres:
        condition: service_healthy
      minio:
        condition: service_healthy
      elasticsearch:
        condition: service_healthy

  ingestion:
    build:
      context: ./ingestion
      dockerfile: Dockerfile
    env_file:
      - ./ingestion/.env
    volumes:
      - ./ingestion:/app
      - shared_uploads:/shared/uploads
    ports:
      - "8001:8000"
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy
      management:
        condition: service_started


  notification:
    build:
      context: ./notification
      dockerfile: Dockerfile
    env_file:
      - ./notification/.env
    volumes:
      - ./notification:/app
    ports:
      - "8003:8000"
    depends_on:
      management:
        condition: service_started
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy
      elasticsearch:
        condition: service_healthy


  rag:
    build:
      context: ./rag
      dockerfile: Dockerfile
    command: daphne -b 0.0.0.0 -p 8000 rag.asgi:application
    env_file:
      - .env
      - ./rag/.env
    environment:
      - REDIS_URL=redis://redis:6379/0
    volumes:
      - ./rag:/app
    ports:
      - "8004:8000"
    depends_on:
      elasticsearch:
        condition: service_healthy
      management:
        condition: service_started
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy


  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    environment:
      - MANAGEMENT_API=http://management:8000
      - INGESTION_API=http://ingestion:8000
      - RAG_API=http://rag:8000
    ports:
      - "3000:3000"
    depends_on:
      management:
        condition: service_started
      ingestion:
        condition: service_started
      rag:
        condition: service_started

  admin-frontend:
    build:
      context: ./admin-frontend
      dockerfile: Dockerfile
    environment:
      - NEXT_PUBLIC_MANAGEMENT_API=http://localhost:8002
      - NEXT_PUBLIC_PORTAL_URL=http://localhost:3000
    ports:
      - "3001:3000"
    depends_on:
      management:
        condition: service_started

  workers:
    build:
      context: ./workers
      dockerfile: Dockerfile
    command: celery -A workers worker --loglevel=info --queues=document_ingestion_jobs,report_generation_jobs,default
    env_file:
      - .env
      - ./workers/.env
    volumes:
      - ./workers:/app
      - shared_uploads:/shared/uploads
    depends_on:
      redis:
        condition: service_healthy
      elasticsearch:
        condition: service_healthy
      management:
        condition: service_started
      postgres:
        condition: service_healthy


volumes:
  esdata:
  shared_uploads:
  pgdata:
  minio-data: